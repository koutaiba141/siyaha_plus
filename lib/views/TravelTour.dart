import 'package:flutter/material.dart';

class TravelTour extends StatelessWidget {
  const TravelTour({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Local Tour Page")),
    );
  }
}
