import 'package:flutter/material.dart';

class LocalTour extends StatelessWidget {
  const LocalTour({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Local Tour Page")),
    );
  }
}
